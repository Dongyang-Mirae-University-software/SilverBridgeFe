# Git 브랜치 & 커밋 명명 규칙

## 브랜치 네이밍 규칙

### 형식

```
type/short-description-author
```

### type 종류

| type       | 설명                       |
| ---------- | -------------------------- |
| `feature`  | 기능 개발                  |
| `fix`      | 버그 수정                  |
| `hotfix`   | 운영 긴급 수정             |
| `refactor` | 리팩토링                   |
| `design`   | UI/디자인 작업             |
| `docs`     | 문서 작업                  |
| `test`     | 테스트 코드                |
| `chore`    | 설정, 패키지, 잡일         |
| `release`  | 배포 준비                  |
| `infra`    | 서버, Docker, Nginx, CI/CD |

### 작성 규칙

- 전부 소문자
- 띄어쓰기 금지
- 단어 구분은 `-` (하이픈)
- 너무 길지 않게
- 기능 단위가 보이게 작성

### 예시

```
feature/login-api
feature/user-profile-edit
fix/signup-validation
hotfix/payment-error
refactor/chat-service
docs/api-spec
infra/docker-nginx-setting
release/v1.2.0
```

---

## 커밋 메시지 규칙

### 형식

```
type: 내용
```

### type 종류

| type       | 설명                                                             |
| ---------- | ---------------------------------------------------------------- |
| `feat`     | 기능 (새로운 기능)                                               |
| `fix`      | 버그 수정                                                        |
| `refactor` | 리팩토링                                                         |
| `design`   | CSS 등 사용자 UI 디자인 변경                                     |
| `comment`  | 필요한 주석 추가 및 변경                                         |
| `style`    | 스타일 (코드 형식, 세미콜론 추가: 비즈니스 로직에 변경 없음)     |
| `docs`     | 문서 수정 (문서 추가, 수정, 삭제, README)                        |
| `test`     | 테스트 (테스트 코드 추가, 수정, 삭제: 비즈니스 로직에 변경 없음) |
| `chore`    | 기타 변경사항 (빌드 스크립트 수정, assets, 패키지 매니저 등)     |
| `init`     | 초기 생성                                                        |
| `rename`   | 파일 혹은 폴더명을 수정하거나 옮기는 작업만 한 경우              |
| `remove`   | 파일을 삭제하는 작업만 수행한 경우                               |

### 커밋 단위 규칙

- **기능 하나 완성 → 즉시 커밋** (작업 뭉텅이로 한 번에 올리지 않습니다)
- 여러 기능을 동시에 수정했더라도 **기능별로 나눠서 따로 커밋**합니다
- `git add` 할 때 관련 파일만 선택하여 스테이징합니다

```
❌ 나쁜 예 — 파일 왕창 올리기
git add .
git commit -m "feat: 여러 기능 작업"

✅ 좋은 예 — 기능별로 나눠서 올리기
git add src/auth/login.service.ts
git commit -m "feat: 로그인 JWT 토큰 발급 로직 추가 ..."

git add src/auth/login.controller.ts
git commit -m "feat: 로그인 API 엔드포인트 추가 ..."
```

### 본문 작성 규칙

- 본문은 `어떻게` 보다 `무엇을`, `왜`를 설명합니다.
- 본문에 여러 줄의 메시지를 작성할 땐 `-`로 구분합니다.
- **커밋 메시지는 전부 한국어로 작성합니다.**
- 커밋 메시지는 **무슨 작업을 했는지 바로 알 수 있게**
- 가능하면 "추가", "수정", "제거"처럼 명확하게
- **본문은 세세하게**: 변경한 파일/함수/로직을 구체적으로 명시
- 추상적인 표현 금지 (❌ "기능 수정" → ✅ "결제 실패 시 재시도 로직 추가")

---

## 커밋 메시지 예시

```
feat: 사용자 로그인 기능 추가
- AuthService에 JWT 액세스 토큰 발급 로직 구현
- 로그인 성공 시 토큰을 응답 헤더에 포함하여 반환
- 로그인 실패(이메일 불일치, 비밀번호 오류) 시 각각 다른 에러 메시지 반환
- 비밀번호는 bcrypt로 해시 비교 처리
```

```
fix: 회원가입 시 중복 이메일 검증 오류 수정
- UserRepository에서 이메일 중복 체크 쿼리 누락으로 동일 이메일 가입 가능하던 문제 해결
- DB users 테이블 email 컬럼에 unique 제약 조건 추가
- 중복 이메일 가입 시도 시 409 Conflict 에러 반환하도록 예외 처리 추가
```

```
refactor: 사용자 서비스 로직 구조 개선
- UserController에 혼재되어 있던 비즈니스 로직을 UserService로 분리
- 중복으로 사용되던 이메일 형식 검증 로직을 validateEmail() 함수로 추출하여 재사용
- UserRepository를 인터페이스로 분리하여 의존성 역전 원칙 적용
```

```
design: 로그인 페이지 UI 변경
- 로그인 버튼 색상을 #3B82F6(파란색)으로 변경 및 hover 효과 추가
- 이메일/비밀번호 입력창 border-radius 4px → 8px로 수정
- 모바일(375px) 기준 반응형 레이아웃 적용, flex → grid로 변경
```

```
style: 코드 포맷 정리 및 세미콜론 통일
- prettier 설정 적용으로 전체 파일 들여쓰기 2칸으로 통일
- 불필요한 trailing comma 및 빈 줄 제거
- 세미콜론 누락된 구문 일괄 추가
```

---

## AI 에이전트 지시사항

> AI는 아래 규칙을 기반으로 브랜치명과 커밋 메시지를 자동 생성합니다.

### 브랜치명 생성 규칙

1. 작업 내용을 파악하여 적절한 `type`을 선택합니다.
2. 핵심 기능을 2~4단어 이내의 소문자 영어로 표현합니다.
3. 단어는 `-`로 구분하며, 필요 시 작성자 이니셜을 끝에 붙입니다.
4. 브랜치명 전체는 소문자만 사용합니다.

### 커밋 메시지 생성 규칙

1. 제목은 `type: 내용` 형식으로 작성합니다.
2. **제목과 본문 모두 한국어로 작성합니다.**
3. **`git add .` 사용 금지** — 반드시 관련 파일만 골라서 스테이징합니다.
4. **기능 단위로 커밋을 나눠서** 각각 메시지를 생성합니다.
   - 작업한 기능이 3개면 커밋도 3개로 분리
   - 파일 여러 개가 하나의 기능에 속하면 함께 묶어서 1커밋
5. 본문은 **세세하게** 작성합니다:
   - 변경된 파일명, 함수명, 클래스명을 구체적으로 명시
   - 변경 이유(왜), 변경 내용(무엇을) 중심으로 서술
   - 추상적 표현 금지 (❌ "로직 수정" → ✅ "결제 실패 시 재시도 횟수 3회로 제한하는 로직 추가")

### 생성 예시 요청 포맷

```
작업 내용: [작업 설명]
작성자: [이름 또는 이니셜] (선택)
```

### 생성 결과 출력 포맷

```
브랜치명: feature/login-api

커밋 메시지 (기능별로 분리):

1.
feat: 로그인 API 엔드포인트 추가
- AuthController에 POST /auth/login 엔드포인트 생성
- 요청 바디에서 이메일, 비밀번호 추출 및 유효성 검사
- 인증 성공 시 200, 실패 시 401 응답 반환

2.
feat: JWT 토큰 발급 서비스 구현
- AuthService에 generateAccessToken() 함수 추가
- 토큰 만료 시간 1시간으로 설정, 페이로드에 userId, email 포함
- 발급된 토큰을 응답 헤더 Authorization에 Bearer 형식으로 포함
```

---

# 프론트엔드 코드 구조 컨벤션

> 리팩토링 작업 중 반복적으로 지시된 규칙을 정리한 가이드입니다. 새 코드를 작성하거나 기존 코드를 옮길 때 아래 기준을 따릅니다.

## 레이어별 책임 분리

각 레이어는 아래 역할만 담당합니다. 다른 레이어의 책임을 섞지 않습니다.

| 위치 | 담당 | 하지 말 것 |
| --- | --- | --- |
| `src/service/api/**` | axios(`apiClient`) 호출 그 자체. 필요하면 응답 언랩까지만 | `useQuery`/`useState` 등 React 훅, UI용 파생 상태 계산 |
| `src/service/query/**` | react-query `queryOptions` / `mutationOptions` 값 정의만 | 내부에서 `useQuery`를 직접 호출하는 커스텀 훅 정의 |
| `src/hooks/**` | `useXxx` 형태의 커스텀 훅 (React state/effect/query 조합) | 컴포넌트 JSX, 순수 계산 로직(별도 유틸로 뺄 것) |
| `src/lib/**` | React에 의존하지 않는 순수 유틸 함수(포맷터, 응답 언랩 등) | 훅, 컴포넌트 |
| `src/components/**` | 여러 화면에서 재사용하는 UI 컴포넌트 | 특정 페이지 전용 로직 |

`service/api` 함수의 이상적인 형태 예시:

```ts
export function setContactUs(body: ISetContactUsProps) {
  return apiClient.post('/contact-us', body).then(res => res.data);
}
```

- 이 프로젝트의 `apiClient`는 응답 인터셉터에서 axios 응답을 이미 한 번 언랩(`response => response.data`)해서 반환합니다. 즉 `apiClient.get<CommonResponse<T>>(...)`의 실제 반환값은 `CommonResponse<T>`이지, `AxiosResponse<CommonResponse<T>>`가 아닙니다. 호출부에서 `.data.data`처럼 두 번 접근하면 항상 `undefined`/`null`이 되는 버그가 납니다.
- 서버 응답이 한 겹으로 오는지 두 겹으로 오는지 확신할 수 없을 때는 `src/lib/api/responseData.ts`의 `getResponseData<T>()` 같은 공용 언랩 유틸을 사용합니다. 도메인별로 동일한 언랩 로직을 중복 작성하지 않습니다.

## 컴포넌트/훅 분리 기준

- 한 컴포넌트 파일 안에 여러 섹션(카드, 탭 콘텐츠 등)이 있으면 섹션 단위로 별도 함수/파일 컴포넌트로 쪼갭니다.
- 두 군데 이상에서 쓰일 가능성이 있는 로직(날짜 포맷, 페이지네이션, "연결된 대상 존재 여부" 같은 파생 상태)은 처음부터 공용 유틸(`src/lib`)이나 공용 훅(`src/hooks`)·공용 컴포넌트(`src/components`)로 분리합니다.
- 이미 존재하는 공용 컴포넌트(`Tabs`, `Pagination` 등)가 있으면 직접 마크업을 새로 만들지 말고 재사용합니다.
- 여러 화면에서 열리는 모달은 각 컴포넌트의 로컬 `useState`로 관리하지 않고 전역 모달 스토어(`openModal`/`onCloseModal`)를 사용합니다.
- 다만 "재사용 가능성"이 있다고 해서 무조건 훅으로 감쌀 필요는 없습니다. 여러 하위 컴포넌트가 같은 값(예: 에러 모달 오픈 함수)을 공유할 때는 상위 컴포넌트가 소유하고 props로 내려주는 방식도 정당한 선택입니다. 과한 추상화보다 상황에 맞는 단순한 구조를 우선합니다.

## 네이밍

- 컴포넌트/파일 이름은 실제 역할을 정확히 반영해야 합니다. 이름이 겹치거나 역할과 어긋나면(예: 레이아웃을 감싸는 컴포넌트인데 이름이 하위 사이드바 컴포넌트와 혼동됨) 관련 파일 전체를 역할에 맞게 리네이밍합니다.

## 데이터 정확성

- 디자인 시안이나 목업 이미지에 있는 필드라도 실제 백엔드 API 스펙에 없는 값(예: 응답 시간, 통화 상대 이름 등 존재하지 않는 필드)은 화면에 지어내지 않습니다. 빠진 이유를 사용자에게 설명하고, 필요하면 백엔드에 필드 추가를 요청할 내용을 정리해 제안합니다.
- 백엔드 스펙에서 명시적으로 금지한 동작(예: 실제 전화가 발신되는 `tel:` 링크)은 절대로 그대로 구현하지 않고, 스펙이 요구하는 대체 UI(목업 화면 등)로 구현합니다.

## 커밋

- 기능 단위로 커밋을 나눕니다. 여러 기능을 한 번에 작업했더라도 `git add`로 관련 파일만 골라 기능별로 각각 커밋합니다.
- 커밋 메시지에 `Co-Authored-By` 등 AI 이름을 넣지 않습니다.
